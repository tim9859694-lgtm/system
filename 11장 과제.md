1번 문제 풀이정답: 5개의 i-node를 읽어야 하며, 순서는 다음과 같습니다.  /(루트 디렉터리) i-node   /usr 디렉터리 i-node   /usr/dev 디렉터리 i-node  
/usr/dev/source 디렉터리 i-node   /usr/dev/source/app.c 파일의 i-node   
상세 풀이:UNIX 파일 시스템에서 특정 파일에 접근하려면 먼저 그 파일의 메타 정보가 있는 i-node를 찾아야 합니다.  
경로명을 해석할 때는 항상 최상위인 루트 디렉터리(/)부터 시작합니다.  
루트의 i-node를 읽어 그 안에 있는 디렉터리 블록에서 usr을 찾고, 다시 usr 세부 항목에서 dev를 찾는 방식으로 트리 구조의 각 계층마다 하나씩 i-node를 거쳐 가야 하므로 총 5개의 i-node를 순서대로 읽게 됩니다. 

2번 문제 풀이정답: 디스크에서 읽어 들이는 블록의 전체 순서는 다음과 같습니다.  
루트 i-node $\rightarrow$ 루트 디렉터리 블록들   /usr의 i-node $\rightarrow$ /usr 디렉터리 블록   
/usr/dev의 i-node $\rightarrow$ /usr/dev 디렉터리 블록   /usr/dev/source의 i-node $\rightarrow$ /usr/dev/source 디렉터리 블록   
/usr/dev/source/app.c의 i-node $\rightarrow$ /usr/dev/source/app.c의 파일 블록   상세 풀이:1번 문제에서 'i-node'만 골라냈다면, 
2번 문제는 i-node와 실제 데이터가 저장된 디스크 블록을 교대로 읽는 전체 메커니즘을 묻는 것입니다.  
UNIX에서는 "i-node를 읽음 $\rightarrow$ 해당 디렉터리의 실제 내용(블록)을 확인하여 다음 하위 디렉터리의 i-node 번호를 알아냄"의 과정을 반복합니다.  
따라서 각 계층마다 i-node를 읽고 바로 이어서 해당 디렉터리 블록을 읽는 과정이 매칭되어 최종 파일 블록까지 도달하게 됩니다.  

3번 문제 풀이정답: i-node에는 파일 크기, 파일 블록의 위치 정보 등 파일 메타 정보들이 들어있습니다. 파일 읽기나 쓰기가 진행되면 
이 파일 메타 정보가 계속해서 액세스(참조)됩니다.
만약 i-node를 메모리에 올리지 않고 디스크에 그대로 둔 상태로 작업을 진행하면, 
파일 데이터 블록을 읽을 때마다 디스크에 있는 i-node도 반복해서 매번 읽어야 하므로 성능이 엄청나게 떨어집니다. 
따라서 파일을 처음 열 때 메모리의 i-node 테이블에 디스크 i-node를 미리 적재(캐싱)해 두는 것이 훨씬 효과적이고 빠릅니다.  
상세 풀이:디스크 접근 속도는 메모리(RAM)에 비해 매우 느립니다.  
파일을 읽을 때 "다음 데이터는 디스크 어디에 있지?"를 알기 위해 i-node에 기록된 '파일 블록 위치 정보'를 수시로 확인해야 합니다.  
이 정보를 매번 디스크에서 가져오면 병목 현상이 생기기 때문에, open() 시스템 호출 시점에 해당 파일의 i-node를 커널 메모리 공간에 복사해 두고 
메모리 속도로 빠르게 조회하기 위함입니다.  
